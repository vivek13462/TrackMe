<div class="container">
    <h1>Nearby Police Info:</h1>
  <table class="table table-bordered">
    <tr>
      <th>Police Department</th>
      <th>Contact no</th>    
      <th>Street</th>
      <th>City</th>
      <th>State</th> 
      <th>Inform Them</th> 
    </tr>
    <tr>
      <td>{{ name}}</td>    
      <td>{{ phone}}</td>
      <td>{{ address}}</td>
      <td>{{ city}}</td>
      <td>{{ state}}</td>
      <td><h3 class="text-center"><button onclick="UpdateStatus()" class="btn btn-primary">via Message</button></h3></td>
    </tr>
  </table>
</div>
